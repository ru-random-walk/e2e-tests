datasource:
  postgres:
    url: ${RW_DB_URL}
    driver-class-name: org.postgresql.Driver
    username: ${RW_DB_USERNAME}
    password: ${RW_DB_PASSWORD} 
    configuration:
      idle-timeout: 10000
      minimum-idle: 1
      maximum-pool-size: 6
